#!/bin/bash
# omarchy-hook runs every file in hooks/<name>.d/ as `bash "$hook"`, so the
# shebang on a Python hook is ignored and it fails on its first import. The
# recolour script therefore lives a directory up, and this stub runs it.
exec /usr/bin/python3 "$HOME/.config/omarchy/hooks/thunderbird-colors.py" "$@"
